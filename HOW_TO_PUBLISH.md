# 如何发布 flv_lzc 到 pub.dev

## 🎯 快速发布（推荐）

使用提供的发布脚本：

```bash
cd /Users/apple/Documents/Explore_Code/flv_lzc
./publish.sh
```

脚本会自动执行：
1. 清理和获取依赖
2. 代码分析
3. Dry-run 测试
4. 询问确认后正式发布

## 📝 手动发布步骤

### 第一步：登录 pub.dev

```bash
flutter pub login
```

这会打开浏览器，使用 Google 账号登录并授权。

### 第二步：测试发布

```bash
cd /Users/apple/Documents/Explore_Code/flv_lzc
flutter pub publish --dry-run
```

检查输出，确保没有错误。警告可以忽略。

### 第三步：正式发布

```bash
flutter pub publish
```

输入 `y` 确认发布。

### 第四步：验证

访问 https://pub.dev/packages/flv_lzc 确认发布成功。

## ⚠️ 发布前检查清单

- [ ] 包名 `flv_lzc` 在 pub.dev 上可用
- [ ] 版本号为 `1.0.0`
- [ ] README.md 完整且正确
- [ ] CHANGELOG.md 已更新
- [ ] LICENSE 文件存在
- [ ] 已登录 pub.dev 账号
- [ ] 网络连接正常

## 🔧 如果发布失败

### 包名冲突
如果 `flv_lzc` 已被占用，需要：
1. 选择新的包名（如 `flv_lzc_player`）
2. 更新 `pubspec.yaml` 中的 name
3. 更新所有文档中的包名
4. 重新运行 `flutter pub get`

### 其他错误
1. 检查错误信息
2. 修复问题
3. 重新运行 dry-run
4. 再次尝试发布

## 📊 发布后

### 立即检查
1. 访问 https://pub.dev/packages/flv_lzc
2. 确认包信息正确
3. 检查文档显示
4. 测试安装：`flutter pub add flv_lzc`

### 持续维护
1. 监控 pub.dev 上的评分和反馈
2. 回复用户问题
3. 修复 Bug
4. 发布更新版本

## 🆕 发布新版本

### 更新版本号
编辑 `pubspec.yaml`：
```yaml
version: 1.0.1  # 或 1.1.0, 2.0.0
```

### 更新 CHANGELOG
在 `CHANGELOG.md` 顶部添加：
```markdown
## 1.0.1 - 2025-01-XX

### Fixed
- 修复了某个 Bug

### Added
- 添加了某个功能
```

### 发布
```bash
flutter pub publish --dry-run
flutter pub publish
```

## 📞 需要帮助？

- 查看 `PUBLISHING.md` 了解详细信息
- 查看 `SUMMARY.md` 了解项目概况
- 访问 https://dart.dev/tools/pub/publishing 查看官方文档

---

**祝发布顺利！** 🎉
